// Arguments passed into this controller can be accessed via the `$.args` object directly or:
var args = $.args;
/*
function checkLogin()
{
	var uText = $.username.value;
	var pText = $.password.value;
	var correctUser = "cdh";
	var correctPass ="0000";
	var
	
	if(uText === correctUser && pText === correctPass); // maybe add in lowercase-check?
	{
		loginSuccesful();
	}
}

function loginSuccesful() 
{
	
}
*/